@* ════════════════════════════════════════════════
   本文件是历史遗留的空壳：MudBlazor 模板自带的 NavMenu 组件已弃用，
   三列式导航（功能 → 项目 → 页面）现在全部内联在 MainLayout.razor
   中渲染（由 ModuleNavigationService 状态驱动），
   因此这里只保留说明注释，不再输出任何标记
   ════════════════════════════════════════════════ *@
